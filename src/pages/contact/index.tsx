import Head from 'next/head';

import { Container } from './styles';

const Contact = () => {
  return (
    <Container id="contact">
      <Head>
        <title>Ádisson · Contato</title>
      </Head>
    </Container>
  );
};

export default Contact;
